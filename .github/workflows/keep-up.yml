    name: Keep Streamlit App Alive

    on:
      schedule:
        - cron: '0 */10 * * *' # Runs every 10 hrs
      workflow_dispatch: 

    jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send request to Streamlit app
        run: |
          curl -s -o /dev/null https://classycloud.streamlit.app